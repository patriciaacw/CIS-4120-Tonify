TONIFY

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
AI Attribution:
I used the code from my hi-fi design in Figma, and then enhanced it using the Figma AI to fix spacing issues, create the demo page, and add accessibility settings.
